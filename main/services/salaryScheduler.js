/*import cron from "node-cron";
import { resetSalaries } from "./salaryService.js";

// Schedule to run at midnight (00:00) on the 26th of every month
cron.schedule("31 12 * * *", async () => {
  console.log("🟢 Running scheduled salary reset...");
  await resetSalaries();
});

console.log("🔵 Salary reset scheduler initialized.");*/
